{
  "name": "mcp-files",
  "version": "1.0.0",
  "description": "A Model Context Protocol (MCP) server written in Go that provides filesystem access tools for AI assistants.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/inspector": "^0.16.2"
  }
}
